# f08-ww-physics-guide

| 项 | 内容 |
|---|---|
| 立项日期 | 2026-09-18 |
| 状态 | 已完成（物理导读已建立） |
| 任务类型 | 物理导读 / RMC 功能问题说明 |
| 任务模式 | C — 深度物理研究与学习 |
| 报告人 | GitHub Copilot |
| 关联知识库条目 | F08 |
| 涉及文件 | 新建 `MLVR_Physics_Guide/02_RMC权重窗/`；引用 F08 审计档案与 RMC WW 源码；不修改 RMC |
| 分支 / 提交 | 根工作区文档变更；尚未提交 |

---

## 1. 目标与范围（① 立项 · Agent 填）

**要做什么**：用面向物理读者的独立专题说明 RMC 当前 WW 的正确物理关系、实际实现链路、已发现的问题及其对结果/MLVR 的影响，避免把“普通案例能运行”误认为“WW 实现可靠”。
**涉及什么**（仓库 / 模块 / 数据）：固定源 forward neutron WW 为主；cell WW、track/point mesh WW、MCNP `WWP/WWE/WWN/WWINP`、split/roulette、粒子 bank、MPI shared mesh 和 adjoint+WW 边界。技术证据入口指向 `20260918_02_f08-weight-window-audit`，不在导读中复制全部源码审计过程。
**怎样算完成**：新增一个 `02_RMC权重窗/README.md` 总览专题，并在根目录专题表登记；正文至少包含：物理对象与无偏关系、RMC 执行流程、问题按“参数/索引/状态/并行/组合”分组、当前可用边界、Stage 3 修复路线和证据链接。未修复问题必须标为审计发现/待复现，不能写成已动态观察到的崩溃或偏差。
**原始材料**（`logs/` 下有什么，原样保存）：本任务采用的 F08 审计档案、源码证据清单和用户对说明范围的拍板记录；不保存聊天转储。

> **模式 C 追加 · 物理解释**：WW 只改变粒子数和单粒子权重，不应改变 tally 期望。roulette 要满足 $P(\text{survive})\,w_{survive}=w_{in}$；split 后所有后代权重期望应等于原粒子权重，并且后代必须保留位置、方向、能量、时间及物理属性。若参数选择、能群/空间索引或 bank 状态破坏这些关系，影响的是物理估计器的无偏性或输运可达性，而不是单纯的输入体验问题。

---

## 2. 做法与证据（② 设计 · ④ 实施 · Agent 填）

**设计 / 定位**（读了什么、查到什么；关键证据给 `文件:行号` 或数据位置）：基于 F08 源码审计，已定位 WWP 参数生命周期、cell WWE 首能群边界、WWMESH 长度、异构 mesh 最大边界、MPI shared offset、split `ParticleAttr`、WWINP 输入完整性和 adjoint+WW 组合边界等问题。物理导读只解释“正确关系—实现偏差—可能影响”，不代替 Stage 3 修复证据。
**方案选择**：先做单篇总览，避免过早拆成多个专题；按物理链路组织，而不是按 C++ 文件组织。暂不加入动态结果，不给出修复后结论，不把 AIMC 的 WW oracle 当作 RMC 证据。
**实施要点**（改了什么；改动快照见 `changes.diff`，无代码改动写“无”）：新增 `MLVR_Physics_Guide/02_RMC权重窗/README.md`；未修改 RMC、reference 或 benchmark。
**验证输出**（贴真实命令与输出；物理结论从严，工程/文档从简）：

```text
新增物理导读：MLVR_Physics_Guide/02_RMC权重窗/README.md
内容覆盖：无偏关系、RMC 执行链路、问题分类、物理影响、MLVR 影响和 Stage 3 修复顺序。
RMC 源码：未修改。
```

**未覆盖到的验证**（如实写；没有就写“无”）：所有 Stage 3 修复后的动态回归、完整 photon/electron WW、WWG 性能、复杂 WWINP 文件兼容性、MPI 多 rank 实测和 adjoint+WW 的最终支持结论。

---

## 3. 决策记录（③ · **人拍板**）

- **决定**（采纳什么方案）：用户要求完成物理导读；采用单篇 `02_RMC权重窗/README.md` 总览，按物理链路组织，不拆分多个子专题。
- **决定人 / 日期**：用户 / 2026-09-18
- **约束**（能不能动接口 / 基准 / 算力预算…）：只写物理导读，不修改 RMC、reference、benchmark；静态审计候选必须标注证据边界。

**变更卡（B/C 模式，各一行）**：
| 风险 | 改动范围（含“不改什么”） | 验证与停止条件 | 回滚 |
|---|---|---|---|
| 导读可能把静态审计候选误写成已证实运行时缺陷 | 只新增 `MLVR_Physics_Guide/02_RMC权重窗/README.md`，不修改 RMC、不宣称修复 | 每条问题标注证据等级和“待动态确认/已源码确认”；Stage 3 修复后再更新结论 | 删除新增专题目录即可 |

> **模式 C 追加 · 人类理解确认**：用户要求完成物理导读，用它说明目前 RMC WW 的物理关系、代码问题和后续修复路线；已批准单篇总览，不将静态审计候选表述为动态已复现缺陷。

> 未拍板前，Agent 不得改动 `../RMC` 下的任何文件。



---

## 4. 结论与边界（⑤ 归档）

- **结论**：已完成 F08 物理导读。专题明确了 roulette/splitting 的无偏关系、RMC 当前 WW 执行链路、参数/索引/状态/并行/组合问题及其物理影响。当前 RMC WW 只能视为“具备可复用算法骨架但尚未无条件放行的接口”，状态继承 F08 的 E — Defect + D — Integration issue。
- **不能推出什么**（边界）：导读以 E1 源码审计为主，不能推出所有输入必然失败、所有候选均已动态复现、adjoint+WW 已支持或 Stage 3 修复后行为正确。
- **遗留 / 下一步**：另立 Stage 3 修复任务，优先处理 WWP 参数、mesh/energy 校验、边界索引、MPI offset、split 状态复制和 adjoint+WW 契约；每项修复后再做针对性状态/索引/物理验证。
- **提交状态**（分支 / commit / 谁 push）：根工作区文档变更；未提交、未 push。

> **模式 C 追加 · 结果解释**：结果支持或否定了哪个假设；通过/失败在物理上说明什么；不可外推的边界在哪。

---

## 5. 过程记录

| 时间 | 操作 / 事件 |
|---|---|
| 2026-09-18 21:44 | 立项 |
| 2026-09-18 | 用户要求完成物理导读；按单篇总览方案建立 `MLVR_Physics_Guide/02_RMC权重窗/README.md` |
| 2026-09-18 | 完成导读并同步根目录专题表；RMC 未修改 |
| 2026-09-19 | 用户要求将 WW 物理算法和代码上下游讨论长期沉淀；补充 point/track mesh、`DoWeightWindows()` roulette/splitting、状态继承和“先 cell 后 mesh”的理解记录 |

> 关键命令、误判与修正、未决分歧都写在这里；人机讨论较深时把要点并入本表，**不再单独建会话纪要文件**。原始聊天转储不要入仓（可能含凭据）。

**证据等级（可选）**：物理/审查结论可标 E0–E4；工程、文档、治理任务可省略。
