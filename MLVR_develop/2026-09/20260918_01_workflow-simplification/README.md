# workflow-simplification

| 项 | 内容 |
|---|---|
| 立项日期 | 2026-09-18 |
| 状态 | 已完成 |
| 任务类型 | 工作区治理 / 工作流精简 |
| 任务模式 | B — 工程协作（默认） |
| 报告人 | 用户 + Agent |
| 关联知识库条目 | 无 |
| 涉及文件 | `MLVR_develop/_template/README.md`、`README.md`、`AGENTS.md`/`CLAUDE.md`、`STATUS.md`（新增，根目录进度看板）、根 `README.md`、`tools/check-archives.sh` |
| 分支 / 提交 | 根工作区 `main` ／ 待提交 |

---

## 1. 目标与范围（① 立项 · Agent 填）

**要做什么**：按用户要求精简工作流——去掉冗余字段、降低验证负担、把“读文档”换成“多提问”；并在根目录新增一页**进度看板**（人不必翻台账就能知道做到哪一步）。
**涉及什么**：任务模板、工作流规范、硬规则、归档检查脚本注释，新增根目录 `STATUS.md`；不动已有 47 个历史档案。
**怎样算完成**：新模板 5 节可用；模式 C 产物从 6 项减为 3 项内嵌；验证分场景与提问约定写入规范与硬规则；`STATUS.md` 能一屏答出“在哪个 Stage / 等你拍板什么 / 最近完成 / 下一步”。
**原始材料**：用户 2026-09-18 反馈（对话记录，要点见第 3 节）。

> **模式 C 追加 · 物理解释**：物理对象与正确关系是什么；假设成立应观察到什么；什么结果会推翻它。（几句话即可，不必成表。）

---

## 2. 做法与证据（② 设计 · ④ 实施 · Agent 填）

**设计 / 定位**：现状冗余点——模板 10 节；模式 C 6 项专属产物（“人类理解”被拆成 4 份表单）；会话纪要单独成文件；归档清单 10 项；证据等级与实验可复现性强制。用户要求：够用优先、相信前人代码、少文档多对话、保留 ABC。
**方案选择**：采纳“中度精简”（用户选定）。放弃“轻度”（去重不彻底）与“重度”（丢失可追溯性）。
**实施要点**：
- `_template/README.md` 重写：205 行 → 80 行，5 节；C 的三项内嵌在对应节。
- `README.md`：模式表、五步表、归档清单（10→6）、证据等级改可选、新增 5c 验证分场景与 5d 提问约定。
- `AGENTS.md`/`CLAUDE.md`：规则 1 简化；新增规则 10（验证分场景）、11（随时问）。
- 历史档案不追溯改造；`check-archives.sh` 已按三要素检查，只改注释。
- 新增根目录 `STATUS.md`：阶段进度（Stage 0–8）/ ⛔ 待拍板 / 最近完成 / 下一步 / 维护方式，保持一屏；
  并挂进五步表 ⑤ 归档、归档清单第 7 项、硬规则 1、根 `README.md` 导航，确保它会被动更新。
**验证输出**：

```text
$ grep -n '^## ' MLVR_develop/_template/README.md
16:## 1. 目标与范围（① 立项 · Agent 填）
27:## 2. 做法与证据（② 设计 · ④ 实施 · Agent 填）
42:## 3. 决策记录（③ · **人拍板**）
61:## 4. 结论与边界（⑤ 归档）
72:## 5. 过程记录

$ bash tools/check-archives.sh | tail -1
汇总: 需处理 0 项；提示 18 项
```

**未覆盖到的验证**：无（治理类改动，模板自测 + 脚本回归即算通过）。

---

## 3. 决策记录（③ · **人拍板**）

- **决定**：采纳“中度精简”；保留 A/B/C 三模式；验证改分场景；Agent 随时提问、宁多勿少。
- **决定人 / 日期**：用户 / 2026-09-18。
- **约束**：不追溯改历史档案；不改 RMC/AIMC；RMC 改动需拍板、基准冻结、不擅自 push 等硬规则继续有效。

**变更卡（B/C 模式，各一行）**：
| 风险 | 改动范围（含“不改什么”） | 验证与停止条件 | 回滚 |
|---|---|---|---|
| 精简过度导致结论无法复核 | 模板与规范文本；不改历史档案与代码 | 新模板自测 + 脚本回归通过 | git 可整体回滚 |

> **模式 C 追加 · 人类理解确认**：把“人现在理解了哪些关系、批准了什么范围”留下来——问答原文或一两句转述均可。

> 未拍板前，Agent 不得改动 `../RMC` 下的任何文件。

---

## 4. 结论与边界（⑤ 归档）

- **结论**：工作流已按“中度精简”改造——模板 5 节、C 产物 3 项内嵌、验证分场景、提问随时；保留 A/B/C 三模式；新增根目录 `STATUS.md` 作为人工进度入口（阶段 / 待拍板 / 最近完成 / 下一步）。
- **不能推出什么**（边界）：精简不改变已完成任务（含 F02 有界 A）的结论效力，也不表示验证可省略。
- **遗留 / 下一步**：启动 F03 快速审查（只读、够用原则），随后进入 Stage 3/4 框架设计。
- **提交状态**：待提交（根工作区 `main`）。

> **模式 C 追加 · 结果解释**：结果支持或否定了哪个假设；通过/失败在物理上说明什么；不可外推的边界在哪。

---

## 5. 过程记录

| 时间 | 操作 / 事件 |
|---|---|
| 2026-09-18 16:30 | 立项；用户四项选择：F03 快速审查+进 Stage 3/4、验证分场景、中度精简、随时问 |
| 2026-09-18 | 模板重写（205→80 行）、规范与硬规则同步、脚本注释修正；验证通过 |
| 2026-09-18 | 用户要求“最上层要有一个能看做到哪一步的文件”→ 新增根目录 `STATUS.md`，并写入五步表、归档清单与硬规则；顺手修正硬规则 2 中“档案第 4 节”的旧节号笔误（已改第 3 节） |

> 关键命令、误判与修正、未决分歧都写在这里；人机讨论较深时把要点并入本表，**不再单独建会话纪要文件**。原始聊天转储不要入仓（可能含凭据）。

**证据等级（可选）**：不适用（治理任务）。
