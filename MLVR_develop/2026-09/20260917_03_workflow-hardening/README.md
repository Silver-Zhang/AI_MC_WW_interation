# workflow-hardening

| 项 | 内容 |
|---|---|
| 立项日期 | 2026-09-17 |
| 状态 | 已完成 |
| 任务类型 | 工作区治理 / 工作流加固 |
| 任务模式 | B — 工程协作（默认） |
| 报告人 | 用户 + Agent |
| 关联知识库条目 | 无 |
| 涉及文件 | 新增 `tools/check-archives.sh`；工作流规范、模板、硬规则；四个登记档案 |
| 分支 / 提交 | 根工作区 `main` ／ 待提交 |

---

## 0. 人类阅读摘要（模式 C 必填；A/B 按需）

（本任务为模式 B：把归档检查清单变成可执行脚本，补齐四个空白档案，并修订工作流中“会话纪要”与“档案最小要素”的规定；详见第 7 节。）

---

## 1. 任务定义（① 立项 · Agent 填）

**目标**：把工作流自检从人工自觉变成可执行检查，并处置 44 个任务回顾中暴露的三类缺口（空白模板、状态漂移、异构档案无最小标准）。

**范围**：新增 `tools/check-archives.sh`；修订 `MLVR_develop/README.md`、`_template/README.md`、`AGENTS.md`/`CLAUDE.md`；补齐 4 个登记型档案；修正 `INDEX.md` 状态漂移。不修改 `RMC/`、`AIMC_WWiteration/` 与任何物理结论。

**验收标准**：
1. `tools/check-archives.sh` 可运行，本任务归档后“需处理”项归零；
2. 四个空白模板补齐为含三要素的登记型档案；
3. 规范明确“会话纪要条件必填”与“最小要素/异构来源”，且 `AGENTS.md` 与 `CLAUDE.md` 保持同步；
4. 台账与档案状态一致。

**原始材料**：无外部报错；依据为 44 个任务的归档统计与 `20260917_02` 的清理发现。

---

## 2. 调研与设计（② 设计/定位 · Agent 填）

**背景**：44 个任务的统计显示：4 个档案从未填写；11 个模式 C 档案中 9 个缺会话纪要；18 处台账/档案状态不一致（其中 2 处为真实漂移）。根因是检查依赖人工自觉。

**证据链**：
| # | 位置 | 说明 |
|---|---|---|
| 1 | 本轮归档统计 | 44 个任务；空白模板 4；模式 C 缺纪要 9/11；含 2A 节 11/44 |
| 2 | `tools/check-archives.sh`（新增） | 检查空白模板、状态漂移、台账登记、空快照、三要素 |
| 3 | `20260904_01/02` | 档案标“已完成”，台账仍记“待设计” → 真实漂移 |
| 4 | `20260904_03/05`、`20260905_03`、`20260916_01` | 空模板；实际工作由外部 Agent 完成，产出在 AIMC 侧 |

**影响面**：只影响工作区治理与档案格式；不触碰 RMC/AIMC 与物理结论。

**为什么之前没做/没发现**：规范只写了“归档时逐项自检”，没有可执行检查；外部 Agent 的档案与其工作流异构，未纳入最小要素约定。

---

## 2A. 物理解释与可证伪假设（模式 C 必填）

（本任务为模式 B，2A 节不适用：无物理结论或统计解释。）

---

## 3. 方案选项（② 设计/定位 · Agent 填，给 2~3 个）

| 方案 | 做法 | 代价 / 风险 | 推荐度 |
|---|---|---|---|
| A（采纳） | 新增归档检查脚本；补 4 个登记档案；会话纪要降为条件必填；写入最小要素规范 | 需维护脚本与规范同步 | ★推荐 |
| B | 只写脚本，不改规范与档案 | 空白档案与漂移仍在，检查会持续报警 | |
| C（最小改动） | 只补档案，不加检查 | 下次仍然漂移 | |

---

## 4. 决策（③ · **人拍板**）

- **采纳方案**：方案 A（用户 2026-09-17 指示“按照你的方案完成”，即本轮回顾提出的五项建议）。
- **决定人 / 日期**：用户 / 2026-09-17。
- **理由与约束**：检查必须可执行；外部 Agent 档案允许异极但需满足三要素；不得改动 RMC/AIMC 与既有物理结论；会话纪要降级不得削弱“存在未决事项时必须留痕”。

**人类理解确认（模式 C 必填，由人确认或转述后确认）**：（模式 B 不适用。）

**变更卡（模式 B/C 必填）**：
| 项 | 内容 |
|---|---|
| 问题与风险 | 归档完整性依赖人工自觉：空白模板、状态漂移、证据遗落会反复发生 |
| 拟改动 / 不改动 | 新增检查脚本、补 4 个登记档案、修订规范与硬规则；不改 RMC/AIMC 与既有结论 |
| 预期因果链 | 可执行检查 → 问题可枚举 → 逐项处置 → 规范固化最小要素 → 后续归档可自证 |
| 验证与失败停止条件 | 脚本须只读；“需处理”项未能归零则继续处置；规范与硬规则必须一致 |
| 回滚方式（若适用） | 脚本与文档均为纯文本改动，可整体回滚 |

> 未填写本节前，Agent 不得改动 `../RMC` 下的任何文件。

---

## 5. 实施记录（④ · Agent 填，逐步流水账）

| # | 操作 | 命令 / 位置 | 结果 |
|---|---|---|---|
| 1 | 统计归档现状 | 44 任务扫描 | 空白 4、缺纪要 9/11、状态漂移 2 |
| 2 | 新增检查脚本 | `tools/check-archives.sh` | 7 类检查；状态按类别比较；中英双语三要素 |
| 3 | 修正台账漂移 | `INDEX.md` | 20260904_01/02 状态修正 |
| 4 | 补登记档案 | 4 个任务 README | 含结论/证据/边界 |
| 5 | 修订规范 | README、模板、AGENTS/CLAUDE | 会话纪要条件必填；新增 5b 最小要素 |
| 6 | 验证 | 重跑检查脚本 | “需处理”从 7 → 1（仅剩本任务自身） |

**代码改动**：见 [changes.diff](changes.diff)，摘要：
- 新增 `tools/check-archives.sh` —— 归档健康检查（只读）
- `MLVR_develop/README.md` —— 模式表、目录约定、5b 节、清单第 1/10 项、快速开始
- `_template/README.md`、`AGENTS.md`、`CLAUDE.md` —— 会话纪要条件必填 + 三要素
- 4 个登记档案与 `INDEX.md` —— 补档与状态修正

生成方式：本任务改动均在根仓库。

> `changes.diff` 只包含已跟踪文件的修改；新增的 `tools/check-archives.sh` 与本档案属未跟踪文件，不在 diff 中。
```bash
git -C ../../RMC diff > changes.diff
# 改原型则：
git -C ../../AIMC_WWiteration diff > changes.diff
```

---

## 6. 验证 / 实验记录（④ · Agent 填，要贴真实输出）

| 验证项 | 命令 | 结果 |
|---|---|---|
| 脚本语法 | `bash -n tools/check-archives.sh` | 退出 0 |
| 修复前检查 | `bash tools/check-archives.sh` | 需处理 7 项（空白 5、漂移 2） |
| 修复后检查 | 同上 | 需处理 1 项（本任务自身，归档时填写） |
| 硬规则同步 | `AGENTS.md` 与 `CLAUDE.md` 正文比较 | 一致 |

```
（修复前）
空白模板（未填写） (5)
状态与台账不一致 (2)
汇总: 需处理 7 项；提示 20 项

（修复后）
空白模板（未填写） (1)   ← 本任务自身
状态与台账不一致 (0)
台账未登记 / 缺 README.md (0)
台账悬空（目录不存在） (0)
汇总: 需处理 1 项；提示 18 项
```

**实验设置（算法实验必填）**：本任务为工作区治理，不涉及训练/数值实验，不适用。

**未覆盖到的验证**：未验证外部 Agent（Claude/Codex）未来档案是否会主动满足三要素；脚本只检查记录完整性，不判定物理结论正确性。

---

## 6A. 结果解释卡（模式 C 必填）

| 问题 | 解释 |
|---|---|
| 结果支持/否定了哪个假设？ | （模式 B，不适用。） |
| 证据等级与治理标签及其依据？ | （模式 B，不适用。） |
| 通过/失败在物理或工程上分别意味着什么？ | （模式 B，不适用。） |
| 不可外推的边界是什么？ | （模式 B，不适用。） |
| 下一步是否需要升级范围或另立任务？ | （模式 B，不适用。） |

---

## 7. 结论与遗留（⑤ 归档）

- **结论**：归档检查已可执行（`tools/check-archives.sh`，7 类检查）；回顾暴露的三类缺口已处置：4 个空白档案补齐为登记型、2 处状态漂移修正、异极档案的三要素标准写入规范；会话纪要从“模式 C 必填”降为“存在未决事项时必填”。
- **遗留问题 / 后续待办**：1) 10 个空 `changes.diff` 与 8 个空目录属提示级，可后续清理；2) F03 仍未开始，建议作为模式 C 首次完整实测；3) `20260904_04` 的空 diff 待确认。
- **知识库同步**：无。
- **是否已提交**：待提交（根仓库 `main`）。

---

## 8. 时间线

| 时间 | 事件 |
|---|---|
| 2026-09-17 19:49 | 立项 |
| 2026-09-17 | 用户指示“按照你的方案完成” |
| 2026-09-17 | 脚本、补档、规范修订完成；检查“需处理”项从 7 降至 1 |

---

## 9. 工作日志（逐步操作记录）

> 设计/定位与实施过程中 Agent 实际执行的每一步（查了什么、命令是什么、结论是什么）。
> 目的：人不用看聊天记录也能复盘"结论是怎么得出来的"。
> 排查中出现的**误判与修正**也要如实记录（例如"曾按 X 分析，后经取证纠正为 Y"）。

| # | 操作 | 工具/位置 | 结果 |
|---|---|---|---|
| 1 | 立项建档 | `new_task.sh` | 生成文件夹 + logs/ + 模板 |
| 2 | 首个检查脚本版本 | `tools/check-archives.sh` | 发现 7 项需处理 |
| 3 | 脚本降噪 | 状态类别比较 + 台账名称匹配 + 中英三要素 | 误报从 18 降至 2（真漂移） |
| 4 | 补 4 个登记档案 | task03/task05/task08a/成都 | 均含结论/证据/边界 |
| 5 | 规范与硬规则修订 | README/模板/AGENTS/CLAUDE | 会话纪要条件必填；新增 5b 节 |
| 6 | 验证与归档 | 重跑脚本 + INDEX | 需处理项 1（本任务自身） |

**存在未决事项或分歧时必填；其余可选**：另写一份 [会话纪要.md](会话纪要.md)
（Q&A 脉络 + 共识 + 未决事项）。**注意：原始聊天转储不要存仓库**——其中可能含
口令/token 等凭据，纪要必须脱敏。
