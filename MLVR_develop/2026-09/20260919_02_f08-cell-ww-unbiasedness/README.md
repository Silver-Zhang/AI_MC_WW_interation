# f08-cell-ww-unbiasedness

| 项 | 内容 |
|---|---|
| 立项日期 | 2026-09-19 |
| 状态 | 已关闭（不做冗余验证） |
| 任务类型 | 物理验证（取消） |
| 任务模式 | C — 深度物理研究与学习 |
| 报告人 | GitHub Copilot |
| 关联知识库条目 | W10 |
| 涉及文件 | 无；不修改 RMC |
| 分支 / 提交 | 无 |

---

## 1. 目标与范围（① 立项 · Agent 填）

**要做什么**：原计划为 cell WW 建立单粒子 roulette/splitting oracle；用户认为该验证对已修复的 WWP 参数生命周期问题多此一举，决定关闭，不实施。
**涉及什么**（仓库 / 模块 / 数据）：无。已有 WWP 修复已由三条 cell-WW 回归和 cross-particle WWP 对照覆盖。
**怎样算完成**：记录取消理由和不外推边界，不修改 RMC。
**原始材料**（`logs/` 下有什么，原样保存）：无。

> **模式 C 追加 · 物理解释**：roulette/splitting 的无偏关系已在既有算法和导读中明确；本任务原拟重复进行粒子级统计验证。用户决定当前应优先处理已审计的实际代码缺陷，避免以额外 oracle 替代后续 mesh 边界与数据布局修复。

---

## 2. 做法与证据（② 设计 · ④ 实施 · Agent 填）

**设计 / 定位**（读了什么、查到什么；关键证据给 `文件:行号` 或数据位置）：原计划直接调用 `DoWeightWindows()` 进行重复随机抽样；在已有 native WWP 修复、MPI-off 构建和 cell-WW 3/3 回归通过后，用户认为额外 oracle 不改变当前修复结论。
**方案选择**（选了什么、放弃了什么、为什么）：关闭本任务；不新增测试代码、不运行大规模统计、不修改 RMC。后续直接处理 mesh WW 的明确数据/索引问题。
**实施要点**（改了什么；改动快照见 `changes.diff`，无代码改动写“无”）：无 RMC 改动。
**验证输出**（贴真实命令与输出；物理结论从严，工程/文档从简）：

```text
未执行：用户决定不做冗余单粒子 oracle。
```

**未覆盖到的验证**（如实写；没有就写“无”）：本任务不新增 roulette/splitting 统计证据；不改变此前 WWP 修复的回归证据范围。

---

## 3. 决策记录（③ · **人拍板**）

- **决定**（采纳什么方案）：用户决定不实施单粒子 oracle，关闭此冗余验证任务。
- **决定人 / 日期**：用户 / 2026-09-19
- **约束**（能不能动接口 / 基准 / 算力预算…）：不修改 RMC、reference、benchmark 或现有测试。

**变更卡（B/C 模式，各一行）**：
| 风险 | 改动范围（含“不改什么”） | 验证与停止条件 | 回滚 |
|---|---|---|---|
| 为已完成的参数生命周期修复重复增加低价值验证 | 只关闭档案；不改 RMC 和测试 | 已记录用户决定与未覆盖边界后停止 | 无 |

> **模式 C 追加 · 人类理解确认**：用户认为无须多此一举地重复验证；接受以已有代码链路、cross-particle WWP 对照和 cell-WW 回归作为本次 WWP 修复的证据，下一步转入 mesh WW 实际问题。

> 未拍板前，Agent 不得改动 `../RMC` 下的任何文件。



---

## 4. 结论与边界（⑤ 归档）

- **结论**：已关闭，不实施单粒子 roulette/splitting oracle。
- **不能推出什么**（边界）：关闭不增加或削弱 WWP 修复的既有回归证据；也不代表完整 WW 无偏性已独立证明。
- **遗留 / 下一步**：进入 mesh WW 的输入数组长度、空间边界 index 和 point/track 事件时序问题。
- **提交状态**（分支 / commit / 谁 push）：无 RMC 变更；根工作区档案待提交。

> **模式 C 追加 · 结果解释**：关闭该任务不是对 roulette/splitting 关系的新物理判断，而是验证投入边界的决定：当前阶段优先修复有明确代码证据的 mesh 问题。

---

## 5. 过程记录

| 时间 | 操作 / 事件 |
|---|---|
| 2026-09-19 19:49 | 立项 |
| 2026-09-19 | 用户决定不做冗余单粒子 oracle，关闭任务；不修改 RMC |

> 关键命令、误判与修正、未决分歧都写在这里；人机讨论较深时把要点并入本表，**不再单独建会话纪要文件**。原始聊天转储不要入仓（可能含凭据）。

**证据等级（可选）**：物理/审查结论可标 E0–E4；工程、文档、治理任务可省略。
