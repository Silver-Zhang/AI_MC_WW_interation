# 当前能力与验证结论

## 结论

RMC 当前 **standard MGACE fixed-source neutron adjoint** 完整能力为：

> **E — Defect：存在已确认实现错误，不能作为完整、可靠的 MLVR 伴随求解基础。**

这是完整能力分级，不等于每一个算例都会失败。W5、W7 已修复并通过针对性验证；W6 仍是未修复的确定性错误。同时有均匀与非均匀双区域的严格受限非裂变代表案例提供局部正证据，这些事实并不矛盾。

## 先区分五类事实

| 类别 | 当前事实 |
|---|---|
| 物理模型选择 | 伴随裂变究竟采用 prompt-only 还是 total 核，需要物理模型明确；两者不能无补偿混用。 |
| 核数据事实 | `c5g7td` 有两组不同 nubar 数据；H/O 回归数据带 photon 群。 |
| 数值抽样方法 | 当前路径按正向总截面飞行，按伴随产生量选择核素/反应，并用权重修正。 |
| 已确认实现错误 | W6 的双核混用仍未修复；W5 的 $1/r$ 与 W7 的无条件 photon 群访问已修复。 |
| 未验证风险 | 强各向异性、混合材料无偏性、裂变 bank 动态行为、一般几何等。 |

## 已确认的负面结论

- **W5（已修复）：** 修复前在局部密度比例 $r\ne1$ 时，伴随碰撞权重多出 $1/r$。当前工作树让散射和裂变权重都用局部总原子密度 $N_0r$ 归一化宏观总截面；同一 V2 输入的首碰撞后权重由约 $2:1:0.5$ 变为严格 $1:1:1$，$r=1$ 原生轨迹逐字节不变。
- **W6：** 双 nubar 数据下，初始化裂变产生核与运行时前驱群抽样核不同，且无重要性补偿；静态/核数据证据确认。动态群频数、bank 和最终响应误差幅度尚未测量。
- **W7（已修复）：** 修复前伴随初始化无条件定位 photon 群；H/O 数据只出现无关 warning，纯中子 `c5g7td` 输入则在第一条 source state 前 SIGSEGV。当前工作树按粒子模式分别定位上限后，同一输入退出 0，完成 10,000 个源历史并记录 12,487 条初始/后继粒子状态。

W6 单独足以阻止完整可裂变能力放行，因此 W5/W7 修复后主分类仍为 E。

## 已有正证据：非常窄的代表案例

### V0：既有回归

当前 SHA 的旧 H/O 回归 1/1 passed，重建结果与 reference 字节一致。它证明该历史用例没有相对 reference 回归，并不证明一般伴随物理正确；日志仍出现 photon 上限 warning。

### V4：两个群对的标量互易性检查

在 $r=1$、`NNUBAR=0`、30 群 H/O 均匀球、各向同性体源、同一区域标量 track-length flux 响应下，前向/伴随交换能群后测试了两个强非对称群对：14→15 与 20→22。每对五个种子、共 20 个运行；合并统计量为

$$z=-0.2587,\qquad z=0.0829.$$

在预设 $|z|\le3$ 标准和当前样本量下，**未检测到显著差异**。

这不能写成严格互易性证明：它不覆盖更多群对、混合材料、非均匀密度、裂变、各向异性、一般边界/几何或不同源—响应。前/伴随使用相同 seed，统计误差合并采用独立估计假设；这也是解释结果时必须保留的边界。

### W5 双区域：跨密度界面的响应级互易性

修复后的代表性验证把同组成 H/O 分成等体积内球和外壳，分别采用 $(r_{inner},r_{outer})=(0.5,2.0)$ 与 $(2.0,0.5)$。正向计算从一个区域均匀发射并在另一区域统计积分径迹响应；伴随计算交换空间区域和能群。两区域等体积，因此源归一化与响应积分的体积因子相同，可直接比较原始响应。

200k/运行的第一批 40 个算例中，四个五种子合并量和总体都满足 $|z|\le3$，但一个单种子为 $z=-4.007$，所以预设的“所有单种子也通过”总判据失败。保留全部原数据后，对同一 40 个组合统一提升到 1M/运行；原离群回归到 $z=-0.986$，20/20 单种子均通过，四组合并为

$$z=1.106,\quad -0.824,\quad 0.400,\quad -0.367,$$

总体逆方差合并为 $z=0.116$。这支持 W5 修复在所测跨界面完整历史中保持响应互易性；精度升级是在观察离群后进行，且前/伴随共用 seed 标签、协方差未单独估计，因此不能冒充事前独立的普遍证明。

### V3：双 nubar 的功效边界

实际数据上的群概率差很小，使用正态近似得到约 $7.33\times10^7$ 个已观测伴随裂变子代的 80% 功效量级。它是规划尺度，**不是精确 multinomial 样本量保证**。第一阶段动态尝试曾在输运开始前被 W7 阻断；W7 修复后路径已经可达，但本次只验证崩溃消失，没有执行该高统计量频数检验，因此 W6 的动态误差仍未量化。

## 受限子域的等级

对于明确排除 W6 的更窄范围——至少非裂变/`NNUBAR=0`、H/O、两个指定群对和当前 track-length tally 设置——可说“均匀与代表性双区域非均匀问题存在局部正证据”，但整体仍应为 **C — Verify**，而不是 A — Ready。W5 修复解除的是权重公式对 $r\ne1$ 的确定性错误；双区域测试补充了跨界面响应级证据，仍不能外推到一般密度 mesh 或任意非均匀场。

尚未覆盖：密度 mesh、非等体积显式归一化、混合材料碰撞估计器、其他群对、强各向异性、可裂变 bank/权重、delayed 路径、AIS/HDF5、continuous-energy、MPI/OpenMP、Windows。

## 对 MLVR 的含义

伴随场若用作重要性或权重窗的依据，修复前 W5 会把局部密度映射成非物理权重缩放；当前修复已消除这个确定性因子。W7 的可用性阻塞也已消除。W6 仍可能扭曲裂变能群的重要性关系，因此局部 smoke、W5/W7 修复或 V4 的通过都不能放行完整可裂变能力。

## 证据入口

- [F02-B 静态复核](../../MLVR_develop/20260824_f02-adjoint-physics-verification/README.md)
- [F02 数值验证](../../MLVR_develop/20260825_f02-adjoint-numerical-verification/README.md)
- [W5 修复与验证](../../MLVR_develop/20260825_f02-w5-local-density-adjoint-weight-fix/README.md)
- [W5 非均匀密度响应级验证](../../MLVR_develop/20260825_f02-w5-nonuniform-density-reciprocity-verification/README.md)
- [W7 修复与验证](../../MLVR_develop/20260825_f02-w7-neutron-only-adjoint-init-fix/README.md)
- [V2 分析脚本](../../MLVR_develop/20260825_f02-adjoint-numerical-verification/cases/v2_w5_density/analyze_source_trace.py)
- [V3 分析脚本](../../MLVR_develop/20260825_f02-adjoint-numerical-verification/cases/v3_double_nubar/analyze_double_nubar.py)
- [V4 分析脚本](../../MLVR_develop/20260825_f02-adjoint-numerical-verification/cases/v4_reciprocity/analyze_reciprocity.py)
- [审查矩阵](../../MLVR_Knowledge/02_RMC功能审查矩阵.md) 与 [问题台账](../../MLVR_Knowledge/06_已知问题与改进建议.md)
